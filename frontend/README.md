# XRPL Remit UI (Group 3)

Vite + React + TypeScript. Visual system matches the measured Plink rebuild layout (original copy and 3D; no Plink assets).

```powershell
npm install
npm run dev
```

Requires the FastAPI backend on `http://127.0.0.1:8000` (override with `VITE_API_URL`).
